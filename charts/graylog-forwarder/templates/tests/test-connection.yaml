apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graylog-forwarder.fullname" . }}-test-connection"
  labels:
    {{- include "graylog-forwarder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "graylog-forwarder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
